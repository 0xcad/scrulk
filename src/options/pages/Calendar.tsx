export function Calendar() {
  return (
    <section>
      <h2>Calendar</h2>
      <p>
        History view will land in slice 6 (survey + calendar). For now this
        page is a placeholder so navigation and routing are wired up.
      </p>
    </section>
  );
}
